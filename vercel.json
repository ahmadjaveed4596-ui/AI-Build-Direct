{
  "buildCommand": "NODE_ENV=production BASE_PATH=/ pnpm --filter @workspace/ahmed-portfolio run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
