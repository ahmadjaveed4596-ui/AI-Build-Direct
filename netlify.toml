[build]
  command = "pnpm install && NODE_ENV=production BASE_PATH=/ pnpm --filter @workspace/ahmed-portfolio run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
